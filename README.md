# PandaPrintsDash
Live MQTT-based dashboard for tracking Bambu AMS filament usage.

## Setup
1. `cp .env.example .env` and fill in your printer details.
2. `docker-compose up -d --build`
3. Access at `http://localhost:3000`
